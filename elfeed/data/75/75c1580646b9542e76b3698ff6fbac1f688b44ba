<!-- SC_OFF --><div class="md"><p>Hi, I hope that I can post my problem here.</p> <p>I have a little problem when I try to use loaded environment variable.</p> <p>I have a .env file that I use to source the shell.</p> <p>.env file content :</p> <pre><code>REDIS_HOST=192.168.1.27 REDIS_PORT=4000 </code></pre> <p>I&#39;m loading the file like this (in bash) : </p> <p>set -a &amp;&amp; source .env &amp;&amp; set +a</p> <p>In Go I&#39;m loading it with this code, defaulting to &quot;normal&quot; var if nothing exist in env variables</p> <pre><code>if RedisHost = os.Getenv(&quot;REDIS_HOST&quot;); RedisHost == &quot;&quot; { RedisHost = &quot;localhost&quot; } if RedisPort = os.Getenv(&quot;REDIS_PORT&quot;); RedisPort == &quot;&quot; { RedisPort = &quot;6379&quot; } </code></pre> <p>Here is the code that show the problem :</p> <pre><code>temp := RedisHost + &quot;:&quot; + RedisPort println(temp) </code></pre> <p>When nothing exist and the var use my default value, everything work fine and the output result is :</p> <pre><code>localhost:6379 </code></pre> <p>However, if I load the env var, this is the ouput :</p> <pre><code>:637968.1.27 </code></pre> <p>At first I though the colon was the problem but it&#39;s not.</p> <p>The problem is that it seems to mess up every concatenate in my code, not only the one using those var.</p> <p>I have uploaded a minimal working case if you want to check it <a href="https://github.com/eraffaelli/EnvVarProblem">here</a></p> <p>Does anybody ever had a problem like this?</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/eraffaelli"> /u/eraffaelli </a> <br/> <span><a href="https://www.reddit.com/r/golang/comments/bwonlv/concatenate_problem_with_env_variable/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/golang/comments/bwonlv/concatenate_problem_with_env_variable/">[comments]</a></span>